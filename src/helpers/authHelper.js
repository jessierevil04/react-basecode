/*
* @description Helper Method to identify if the User is Authenticated
* @param component - The component this helper method is called
* @param state - The component's state
*/
export const isUserAuthenticated = (component, state) => {
    return true;
}
